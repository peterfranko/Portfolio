# Peter Franko site — Claude Code entry point

Tertiary downstream professional calling card (`pf-portfolio` in prose). Keep its docs lean and defer product meaning to `/Users/peter/Developer/COMPASS.md` and the app contexts.

Read `AGENTS.md` in this folder for the full read order. Standing guardrail: no Studio Spreza links, "currently building" claims, app teasers, or founder/studio bridges unless Peter explicitly changes the confidentiality strategy.

**Source control: Peter commits, agents never do.** This repo is Git-backed and GitHub Pages hosted, and that is exactly why the rule needs saying here. Read git history freely (`status`, `log`, `diff`, `show`, `blame`); never `commit`, `push`, stage, merge, rebase, reset, switch branches, tag, or open a PR, not even for a finished and verified change, and not via a branch or worktree. Edit the working tree, then hand it over with the file list and a suggested commit message. The older "use normal Git workflow here" licence for `GitHub/` was withdrawn 2026-09-03.

Workspace rules, including the Simulator and device boundary: `/Users/peter/Developer/CLAUDE.md`.
